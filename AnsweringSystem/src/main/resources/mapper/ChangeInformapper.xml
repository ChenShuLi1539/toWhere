<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gangoffive.project.demo.mapper.ChangeInformapper">


    <update id="courtyDeleteManageDepart">
        UPDATE Administer SET departmentId = null WHERE Id = #{id}
    </update>
    <update id="allocateDepart">
         UPDATE Administer SET departmentId = #{departmentId} WHERE Id = #{id}
    </update>
</mapper>
